the model is: once the data is trained, the model is just the predictor. breaking down the pikl files won't help

we are more interested in the training & feature importance section

dates start at sep 1st - august 10th is earliest data point, but not used --> id 0  is sep 1st 2017

start by running all the data thru on a yearly basis

example: can we guarantee that line 0 is going to be sep 1st and that 241 is going to be April 20th?
